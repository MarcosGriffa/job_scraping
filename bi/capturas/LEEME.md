# Capturas del dashboard

Acá va **una sola imagen**: `dashboard-portfolio.png`, la captura final
del dashboard de Power BI ya armado (ver `bi/README.md`, Paso 7).

No se sube nada más de Power BI a esta carpeta ni al repo — ni el
`.pbix`, ni exports de datos, ni capturas intermedias. Solo esa imagen
terminada, que es lo que un reclutador va a ver sin necesitar tener
Power BI Desktop instalado.

Este archivo (`LEEME.md`) se puede borrar en cuanto se agregue la
imagen real — solo existe para que la carpeta no quede vacía en Git
mientras tanto.
